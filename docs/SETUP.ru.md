# Инструкция по запуску проекта

## Установка зависимостей

```bash
npm install
```

## Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните необходимые ключи:

```bash
cp .env.example .env
```

Необходимые переменные:

- `KINOPOISK_API_KEY_1` - API ключ от Kinopoisk API (https://kinopoiskapiunofficial.tech/)
- Дополнительные ключи `KINOPOISK_API_KEY_2`, `KINOPOISK_API_KEY_3` и т.д. для ротации при исчерпании лимитов

## Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу:

- Русская версия: http://localhost:3000/ru
- Английская версия: http://localhost:3000/en

## Сборка для продакшена

```bash
npm run build
npm start
```

## Особенности многоязычной версии

### Автоматический перевод

Контент с API Kinopoisk (только на русском) автоматически переводится на английский через Google Translate API при переключении языка.

### Структура URL

Все страницы имеют префикс языка:

- `/ru/` - русская версия
- `/en/` - английская версия

### Переключение языка

Пользователи могут переключать язык через выпадающее меню в шапке сайта (иконка 🌐).

## Основные страницы

- `/[locale]/` - Главная страница с рекомендациями
- `/[locale]/about/[id]` - Детальная информация о фильме
- `/[locale]/watch/[id]` - Просмотр фильма
- `/[locale]/search` - Поиск фильмов
- `/[locale]/top` - Топ фильмов (250 лучших, популярные, ожидаемые)
- `/[locale]/favorites` - Избранные фильмы (требует авторизации)
- `/[locale]/history` - История просмотров (требует авторизации)

## Технологии

- **Next.js 16** - React фреймворк
- **next-intl** - Интернационализация
- **Tailwind CSS** - Стилизация
- **shadcn/ui** - UI компоненты
- **Kinopoisk API** - Данные о фильмах
- **Google Translate API** - Автоматический перевод
- **Stack Auth** - Авторизация
- **Prisma** - ORM для базы данных

## Структура проекта

```
src/
├── app/
│   ├── [locale]/          # Страницы с поддержкой языков
│   │   ├── about/
│   │   ├── watch/
│   │   ├── search/
│   │   ├── top/
│   │   ├── favorites/
│   │   ├── history/
│   │   ├── layout.tsx
│   │   └── page.tsx
│   ├── api/               # API роуты
│   ├── layout.tsx         # Корневой layout
│   └── globals.css
├── components/            # React компоненты
│   ├── ui/               # shadcn/ui компоненты
│   ├── Header.tsx
│   ├── Hero.tsx
│   └── NavigationPanel.tsx
├── lib/                  # Утилиты
│   ├── api.ts           # Kinopoisk API клиент
│   ├── translate.ts     # Функции перевода
│   └── utils.ts
├── i18n/                # Конфигурация интернационализации
│   ├── routing.ts
│   └── request.ts
└── middleware.ts        # Next.js middleware для роутинга

messages/                # Файлы переводов
├── ru.json
└── en.json
```

## Troubleshooting

### Ошибка "API Key exhausted"

Добавьте дополнительные API ключи в `.env` файл (`KINOPOISK_API_KEY_2`, `KINOPOISK_API_KEY_3` и т.д.)

### Не работает перевод

Проверьте доступность Google Translate API. При ошибке перевода отображается оригинальный текст на русском.

### Проблемы с роутингом

Убедитесь, что все ссылки используют компонент `Link` из `@/i18n/routing`, а не из `next/link`.
