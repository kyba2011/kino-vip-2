# Translation Cache - Quick Start

## Что это? / What is it?

Система автоматического кеширования переводов для ускорения работы сайта.

Automatic translation caching system to speed up the website.

## Как это работает? / How does it work?

1. **Первый визит** - переводы запрашиваются с Google Translate API
2. **Сохранение** - переводы сохраняются в localStorage браузера
3. **Следующий визит** - переводы загружаются из кеша мгновенно

## Преимущества / Benefits

✅ **Быстрее** - мгновенная загрузка переведенного контента  
✅ **Экономия** - меньше запросов к API  
✅ **Офлайн** - переводы доступны без интернета  
✅ **Автоматически** - не требует настройки

## Пример использования / Usage Example

### До кеширования / Before Caching

```
Загрузка страницы: 3-5 секунд
Переводов: 50 запросов к API
```

### После кеширования / After Caching

```
Загрузка страницы: 0.5-1 секунда
Переводов: 0 запросов к API (из кеша)
```

## Мониторинг / Monitoring

Добавьте компонент для просмотра статистики:

```tsx
import TranslationCacheDebug from "@/components/TranslationCacheDebug";

<TranslationCacheDebug />;
```

## Очистка кеша / Clear Cache

Если нужно очистить кеш:

```typescript
import { clearTranslationCache } from "@/lib/translate";

clearTranslationCache();
```

Или через DevTools:

```javascript
localStorage.removeItem("kino_translations_cache");
```

## FAQ

**Q: Сколько места занимает кеш?**  
A: Обычно 100-500 KB, максимум ~2 MB

**Q: Когда кеш очищается?**  
A: Автоматически при достижении 1000 записей

**Q: Можно ли отключить кеш?**  
A: Да, просто не используйте функции из `translate.ts`

**Q: Кеш работает между устройствами?**  
A: Нет, кеш локальный для каждого браузера

---

**Готово!** Кеширование работает автоматически 🚀
